/** In-scope reader copy (locales stay untouched). */

export const readerCopy = {
  empty: "No article to display.",
  aborted: "Image download was cancelled.",
  timeout: "Image download timed out.",
} as const;
